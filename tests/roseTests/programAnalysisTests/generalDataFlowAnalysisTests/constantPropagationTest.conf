# Test configuration file (see "scripts/rth_run.pl --help" for details)

cmd = ./fuse ${ROSE_FLAGS} -c ${CONST_PROP_INCL} ${INPUT};
timeout = 4m
may_fail = ${CONFDIR}/cp_test_status.txt:promote
lockdir = .